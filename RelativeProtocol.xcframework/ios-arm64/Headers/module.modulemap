module RelativeProtocol {
  umbrella header "RelativeProtocol.h"

  // Explicit header declarations for better module resolution
  header "core/types.h"
  header "api/relative_vpn.h"

  export *
  module * { export * }

  link "relative_vpn-arm64"
}
